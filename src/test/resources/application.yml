spring:
  datasource:
    url: jdbc:h2:mem:testdb   # 메모리 기반 H2 테스트 DB
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop   # 테스트 시작 시 테이블 생성, 끝나면 삭제
    show-sql: true
  h2:
    console:
      enabled: true
